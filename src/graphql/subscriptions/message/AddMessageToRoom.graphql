subscription CounselorMessageAdded {
  counselorMessageAdded {
    id
    text
    createdAt
    messagable {
      __typename
      ... on User {
        ...User
      }
      ... on Counselor {
        ...Counselor
      }
    }
  }
}
