mutation UserVerification(
  $signInToken: String!
  $companyId: ID!
  $deviceId: ID!
) {
  verifyUser(
    input: {
      signInToken: $signInToken
      companyId: $companyId
      deviceId: $deviceId
    }
  ) {
    errors {
      ...Error
    }
    jwt
    user {
      ...User
    }
  }
}
